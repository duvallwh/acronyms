# Acronym Extractor Frontend

This Vite app provides the React interface for the acronym extraction API.

## Development

```bash
npm install
npm run dev
```

The dev server proxies `/api` requests to `http://127.0.0.1:8000`.
